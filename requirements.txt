# Основные зависимости LangChain и LangGraph
langchain==0.3.24
langchain-core==0.3.56
langgraph==0.3.34

# --- Yandex --- Добавлено/Обновлено ---
langchain-community>=0.0.56 # Убедимся, что версия свежая для адаптеров Яндекса
yandexcloud>=0.218.0 # Yandex Cloud SDK (может пригодиться, хотя адаптеры важнее)

# LLM/Embeddings (УДАЛЯЕМ OpenAI, если переходим полностью на Yandex)
# langchain-openai==0.3.14 # Убираем

# База данных
sqlalchemy==2.0.40
psycopg2-binary==2.9.10
pgvector==0.4.0
alembic==1.15.2
aiosqlite==0.21.0 # Добавлена зависимость для AsyncSqliteSaver

# Telegram
python-telegram-bot==22.0

# Утилиты
python-dotenv==1.0.0
pyyaml==6.0.2
requests==2.31.0
portalocker==2.8.2

# --- OCR (Опциональные зависимости) ---
# Если функция распознавания текста из изображений не нужна, эти зависимости можно удалить.
easyocr==1.7.2
torch==2.7.0              # Большая зависимость, основная для PyTorch
torchvision==0.18.0        # Рекомендуется указать версию, совместимую с torch
torchaudio==2.3.0         # Рекомендуется указать версию, совместимую с torch
opencv-python-headless==4.11.0.86
Pillow==11.2.1             # Часто является зависимостью easyocr/torchvision
numpy==2.2.5               # Часто является зависимостью torch/opencv
scipy                       # Зависимость easyocr (версия будет подобрана или можно указать явно)
scikit-image                # Зависимость easyocr (версия будет подобрана или можно указать явно)
pyclipper                   # Зависимость easyocr
shapely                     # Зависимость easyocr
# --- Конец блока OCR ---

# Тестирование
pytest==8.0.2
pytest-asyncio==0.23.5

# Кеширование
redis==5.0.7

# Прочее (если есть)
annotated-types==0.7.0
anyio==4.9.0
beautifulsoup4==4.12.3
certifi==2025.1.31
charset-normalizer==3.4.1
langchain-tavily==0.0.4 # Добавляем Tavily

# Добавляем prometheus-client из ветки old-main
prometheus-client==2.3.1 # или актуальная версия, если знаешь


